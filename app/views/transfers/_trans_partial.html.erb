<% transfer_list.each do |transfer| %>

<table class="table">
  <tr>
    <th>Mode</th>
    <th>Name dealer</th>
    <th>Date output</th>
    <th>Date supply</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <tr>
    <td><%= transfer.mode %></td>
    <td><%= transfer.name_dealer %></td>
    <td><%= transfer.date_output %></td>
    <td><%= transfer.date_supply %></td>
    <td><%= link_to 'Show', branch_transfer_path(@branch,transfer), :class=>"btn btn-primary" %></td>
    <td><%= link_to 'Edit', edit_branch_transfer_path(@branch,transfer),:class=>"btn btn-primary"  %></td>
    <td><%#= link_to 'Destroy', transfer, confirm: 'Are you sure?', method: :delete %></td>
  </tr>

</table>


     <br/>


<% end  %>
