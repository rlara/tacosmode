<%= form_for(@transfered_product) do |f| %>
  <% if @transfered_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transfered_product.errors.count, "error") %> prohibited this transfered_product from being saved:</h2>

      <ul>
      <% @transfered_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Transfers %><br />
    <%= f.text_field :Transfers %>
  </div>
  <div class="field">
    <%= f.label :products %><br />
    <%= f.text_field :products %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
